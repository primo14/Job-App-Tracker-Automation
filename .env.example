# Required
Notion_API_KEY=
OPENAI_API_KEY=
Database_Id=

# Optional — only needed to point Selenium at a non-default browser (e.g. Brave)
# instead of a standard Chrome/Chromium install.
Browser_Executable_Path=
